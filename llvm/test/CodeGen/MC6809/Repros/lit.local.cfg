# Repros/ holds standalone, human-readable C reproducers for bugs
# that need the full runtime (picolibc + usim) to observe.
# They are not hermetic lit tests and shouldn't be discovered by
# `llvm-lit`. Keep them here for bisect/history and run them manually
# via picolibc's scripts/run-mc6809 harness.
config.suffixes = []
